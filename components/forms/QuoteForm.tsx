'use client';

// Placeholder de la Task 13 — el formulario real por pasos llega ahí.
import type { BusinessHours, Service } from '@/lib/types';

export function QuoteForm(_props: { services: Service[]; hours: BusinessHours }) {
  return <div data-placeholder="quote-form" />;
}
